[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python -m venv .venv", ".venv/bin/pip install -r requirements.txt"]

[start]
cmd = ".venv/bin/python -m streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
